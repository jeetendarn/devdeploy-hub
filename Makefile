## Docker Progress

### Completed

- Docker installed
- Docker service verified
- First image pulled
- First container executed
- Docker CLI basics learned

### Docker Commands Practiced

```bash
docker --version
docker compose version
docker info
docker pull hello-world
docker run hello-world
docker images
docker ps
docker ps -a
docker logs
docker stop
docker start
docker rm
docker rmi
docker system df
docker image prune
docker container prune
```